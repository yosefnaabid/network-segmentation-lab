---
# Inventario del lab. Las IPs son las del plan de direccionamiento (spec §4).
# El grupo "alma" agrupa todos los AlmaLinux: el enganche para el rol
# wazuh_agent de la fase 2 es una linea en sus children (spec §13).
all:
  children:
    servers:
      hosts:
        srv-dns:
          ansible_host: 10.10.20.10
          lab_gateway: 10.10.20.1
        srv-app:
          ansible_host: 10.10.20.20
          lab_gateway: 10.10.20.1
    dmz:
      hosts:
        dmz-web:
          ansible_host: 10.10.30.10
          lab_gateway: 10.10.30.1
    mgmt:
      hosts:
        jump01:
          ansible_host: 10.10.50.10
          lab_gateway: 10.10.50.1
    alma:
      children:
        servers:
        dmz:
        mgmt:
