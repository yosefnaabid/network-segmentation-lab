---
# Colecciones que usan los roles. Instalar con:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.postgresql
    version: ">=3.4.0"
